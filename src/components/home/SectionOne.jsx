import bgLines from '../../assets/backLines.png'
const SectionOne = () => {
    return (
        <div className={`h-[600px] md:min-h-screen w-full bg-white bgTwo `}>

        </div>
    );
};

export default SectionOne;